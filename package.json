{
	"name": "sn-ajax-adapter",
	"version": "1.2.0",
	"private": true,
	"description": "Type-safe, boilerplate-free GlideAjax for ServiceNow. This package exists only to run the test suite; the deliverables are the two files in src/.",
	"type": "module",
	"scripts": {
		"test": "vitest run",
		"test:watch": "vitest",
		"typecheck": "tsc --noEmit"
	},
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.14.0",
		"typescript": "^5.5.0",
		"vitest": "^2.0.0"
	}
}
